# 更新日志

本项目所有重要变更都会记录在本文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 计划中

- （暂无）

## [1.0.0] - 2026-09-18

首个公开发布版本。

### 新增

- **后台管理**：表单创建 / 编辑 / 删除 / 克隆，一键发布与关闭收集，内置表单模板库（活动报名 / 满意度调查 / 信息登记 / 反馈收集一键套用）
- **字段设计器**：11 种字段类型——单行文本、多行文本、数字、邮箱、手机号、日期、单选、多选、下拉选择、文件上传、地区（省市县联动，内置区划数据）；可配置约束（字符数、正则、数值/日期范围、多选数量限制、文件大小与类型），前后端双重校验
- **数据查看**：分页列表（动态列）、关键词搜索、按字段筛选 + 提交时间范围筛选、单条详情弹窗（含附件下载）、删除单条、清空全部
- **字段显示开关**：每个字段可单独控制是否出现在「数据列表」「筛选条件」「统计分析」中（详情 / 导出 / Webhook 始终包含全部字段）
- **统计分析**：总提交 / 今日 / 近 7 天 / 独立 IP 概览；时间范围可切换（近 7/14/30 天或自定义起止）；趋势图、选项分布占比、数字均值最值、文本高频值、各字段填写率
- **导出**：CSV（带 BOM）与 Excel（xlsx），同步携带当前的搜索、字段筛选与时间范围
- **Webhook**：独立预设管理页，支持自定义请求方式（GET / POST / PUT）与请求数据模板（占位符 `{{title}}` `{{formId}}` `{{submissionId}}` `{{time}}` `{{fields}}` `{{json}}`），表单设置中可勾选启用多个预设，删除预设自动解除引用；旧版按表单直填的 Webhook 地址自动迁移为预设
- **填写链接二维码**：后台一键生成填写页二维码（SVG，无外部服务）
- **IP 限制**：可设置「同一 IP 提交上限」（0 = 不限制），达到上限的 IP 无法再次提交
- **前台首页**：`/` 为「表单中心」，卡片式列出已发布且勾选首页展示的表单
- **多管理员与权限**：超级管理员 / 编辑 / 观察者三种角色，接口级权限校验，角色修改即时生效；保护规则（不能删除当前登录账号、至少保留一名超管）
- **审计日志**：记录全部管理操作，仅超管可查（查看最近 500 条，自动保留最近 2000 条）
- **登录验证码**：SVG 图形验证码，5 分钟有效、一次性使用，无需图像库或外部服务
- **公开填写页 `/f/:slug`**：服务端渲染字段结构；文件上传（大小 / 类型限制）；草稿自动保存（localStorage，可一键清空）；草稿 / 已关闭 / 达到 IP 上限状态自动提示
- **数据存储**：SQLite 单文件 `data/form.db`（Bun 内置 `bun:sqlite`，零原生依赖），WAL 模式 + 外键约束 + busy_timeout，表单结构与提交数据以 JSON 存储；上传附件按表单目录存储（`data/uploads/form-{表单ID}/`），仅能通过后台接口下载（防目录穿越）
- **客户端 IP 识别**：优先读取可信反向代理的 `X-Forwarded-For` / `X-Real-IP`，通过 `TRUSTED_PROXIES` 白名单防伪造

### 安全

- 登录失败锁定：同 IP + 用户名 连续失败 5 次锁定 15 分钟；登录 / 验证码 / 提交接口均有限流
- 公开提交防护：蜜罐字段命中静默丢弃；提交过快（< 2 秒）返回明确错误；请求体总量 64MB / 单次最多 20 个文件（超出返回 413），校验失败时已保存的上传文件自动清理
- CSRF 防护：后台写请求（非 GET/HEAD）必须携带与 httpOnly Cookie 配对的 `X-CSRF-Token` 头；登录态与 CSRF Cookie 在 HTTPS 下自动带 `Secure` 标记
- 密码哈希使用 Bun 内置 argon2id；自改密码需验证旧密码；默认管理员密码不回显到控制台 / 登录页
- 生产环境（`NODE_ENV=production`）未显式设置 `ADMIN_SECRET` 时拒绝启动
- 字段 id 仅允许字母数字下划线连字符（保存时强制清洗，非法 id 自动重新生成），防止注入前端 Alpine 表达式（XSS）；内嵌 JSON 转义 `<` 防止 `</script>` 提前闭合
- `xlsx` 固定使用 SheetJS 官方 CDN 的 0.20.3（npm 上的 0.18.5 存在已知漏洞 CVE-2023-30533 / CVE-2024-22363 且不再更新）

### 技术栈

- Bun + Hono + SQLite（`bun:sqlite`）+ EJS（服务端模板）+ Alpine.js（本地分发于 `/vendor/alpine.js`）
- 全程无构建步骤，页面由 EJS 服务端渲染，Alpine.js 负责前端交互，不依赖外网 CDN
